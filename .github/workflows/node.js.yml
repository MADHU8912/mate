name: Build Code

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Install Packages
        run: npm install

      - name: Build Project
        run: echo "Build Successful"
